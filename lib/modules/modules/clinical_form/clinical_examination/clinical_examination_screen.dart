import 'package:flutter/material.dart';

class ClinicalExaminationScreen extends StatelessWidget {
  const ClinicalExaminationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
